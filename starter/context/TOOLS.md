---
contract: My toolbelt — everything I can reach for to do work. CLIs on PATH, MCP tool families, secrets I can read, repo-local helpers, and explicit nopes. ADD on install, REMOVE on uninstall. Each entry: name, when to reach for it, one example.
---

# Tools

<!--
Group by category. Each entry: tool name, when to use it, one-line example.

## Shell / system
- `gh` — GitHub CLI. Use for PRs, issues, releases. `gh pr create`.
- `jq` — JSON parsing in shell. `jq '.field' file.json`.

## APIs / services
- `$OPENAI_API_KEY` — set, can curl OpenAI directly.

## Local helpers
- `scripts/deploy.sh` — deploys the blog. Run from repo root.

## Explicit nopes
- Never use `sudo` — never needed in this sandbox.
-->
